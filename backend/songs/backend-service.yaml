apiVersion: v1
kind: Service
metadata:
  name: songs-backend-service
  labels:
    app: songs-backend-service
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: songs-backend